//- progress - object{progress}
mixin progress-bar-circle(progress)
  // Circle Progress bar-circle
  .progress-bar-circle(data-value= progress.value, data-gradient= progress.colorFill, data-empty-fill= progress.colorEmpty, data-size= progress.circleSize )
    span